{
  "name": "flowforge",
  "private": true,
  "version": "1.0.0",
  "description": "FlowForge — workflow automation platform (Node production stack)",
  "scripts": {
    "install:all": "npm install --prefix backend-node && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix backend-node",
    "dev": "npm run dev --prefix backend-node & npm run dev --prefix frontend",
    "dev:api": "npm run dev --prefix backend-node",
    "dev:web": "npm run dev --prefix frontend"
  },
  "engines": {
    "node": ">=18"
  }
}
